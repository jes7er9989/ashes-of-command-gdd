# CC-126 — Dashboard & Root Shell Audit

**Scope:** `pages/chapters/dashboard.html`, `index.html`
**Date:** 2026-04-05
**Mode:** READ-ONLY

## Summary
2 files audited across all 6 axes. 9 findings total: 0 CRITICAL, 3 MAJOR, 4 MINOR, 2 INFO. The dashboard content is canon-accurate. The root `index.html` has a significantly stale script-loading comment block (6 scripts undocumented, wrong loader name, wrong count).

## 1. Lore Mismatches
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| — | — | — | No lore mismatches found. | — |

## 2. Visual/Style Issues
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| — | — | — | No visual/style issues found. | — |

## 3. Inaccuracies
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| A1 | MAJOR | index.html:226 | Comment says "15 files total" but 21 `<script>` tags are actually loaded. | Update comment to "21 files total". |
| A2 | MAJOR | index.html:227 | Comment says "0. three.min.js (CDN)" but the actual tag loads local `js/three-loader.js`, not a CDN script. | Update comment to match actual file name and note it is local. |
| A3 | MAJOR | index.html:226-248 | Script manifest comment is missing 6 scripts that are actually loaded: `planet-textures.js`, `planet-renderer.js`, `decrypt-reveal.js`, `music-player.js`, `canvas-galaxy.js`, and misnaming `three-loader.js`. | Add the 6 missing scripts to the numbered comment list with dependency notes. |

## 4. Code Issues
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| C1 | MINOR | dashboard.html:1 | `<canvas id="dashboard-canvas">` and surrounding divs are outside any `<div class="page-dashboard">` wrapper; the closing `</div><!-- /page-dashboard -->` at line 246 has no matching open tag in this fragment. | Verify the open tag is emitted by `Dashboard.render()` before this fragment is injected, or add the wrapper. |
| C2 | MINOR | index.html:451 | `e.userChoice.then(...)` — the `beforeinstallprompt` event's `userChoice` property is only available after `prompt()` is called; accessing it immediately may silently fail on some browsers. | Move `.userChoice.then()` into the install button click handler after `prompt()` is called. |

## 5. Leftover/Stale Content
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| S1 | MINOR | index.html:436 | Comment `// Back-to-top button handled by Nav.bindBackToTop()` is orphaned — the logic is inside `nav.js` but this comment sits alone with no surrounding code, making it look like a removed block. | Remove the orphaned comment or move it to the boot sequence summary. |
| S2 | MINOR | index.html:226-248 | The script manifest block refers to "r128" Three.js version and "CDN" sourcing, neither of which applies to the current `three-loader.js` implementation. | Update the manifest to reflect the actual loading strategy. |

## 6. Missing Details
| # | Severity | File:Line | Finding | Recommended Fix |
|---|----------|-----------|---------|-----------------|
| M1 | INFO | dashboard.html:28-59 | The "By the Numbers" stats grid shows 6 stats (Factions, Units, Equipment, Tech Nodes, Planet Types, Decisions) but omits the canon "140 Buildings (20/faction × 7)" stat. | Consider adding a Buildings stat block for completeness, or note as intentional scope. |
| M2 | INFO | dashboard.html:65-68 | The Territory Control Map SVG `#territory-map` is empty in the static fragment (comment says "Generated by JS on init") — if JS fails to load, users see a blank box with no fallback or `<text>` placeholder. | Add a `<text>` fallback inside the SVG (e.g., "Map loading…") for graceful degradation. |

## Clean Files (no findings)
- *(Neither file is fully clean; both have findings.)*
